# 火电两票协同智能审查与设备运维多 Agent 系统

- 版本：v1.0 定稿（2026-08-10）
- 架构形态：事件驱动 + 编排式多 Agent（Orchestrated）+ 人机协同 + 知识闭环
- 内部材料（人设口径/简历措辞）：见 INTERNAL.md，严禁上 GitHub

---

## 1. 项目概述 & 定位 & 面试一句话开场白

面向火电厂设备运维与安全生产场景，构建事件驱动的多 Agent 协同系统：24 小时设备状态感知、跨班组故障会诊、两票（工作票/操作票）智能辅助审查、处置闭环与知识沉淀。全程规则兜底、人工分级确认，LLM 只辅助不决策；断网私有化部署，满足电力数据安全与等保合规要求。

**面试一句话开场白**：
"这是一个事件驱动的多 Agent 系统：监测 Agent 24 小时感知设备状态，异常时自动唤醒班组专家并行会诊，主控整合研判素材生成参考方案，两票 Agent 辅助新人合规填票，复盘 Agent 沉淀知识闭环；全程规则兜底、人工分级确认，LLM 只辅助不决策，私有化部署适配电厂数据安全要求。它解决的是从'报警'到'辅助决策'的完整链路问题。"

## 2. 核心设计原则（铁律）

1. **无 AI 决策**：所有结论均为参考，最终结论由运维人员决定。
2. **受控自主**：编排式多 Agent——确定性交给代码（路由/门控/校验），智能性交给 LLM（诊断/整合/审查），底线交给人工（分级确认）。
3. **私有化合规**：现场断网私有化运行（72B + vLLM），数据不出域，满足电力等保合规。
4. **算力按需使用**：规则引擎常驻监测，LLM 仅在越限/组合异常时启动；趋势预警不触发诊断；动态路由只激活相关班组。
5. **知识闭环**：人工确认后入库，系统越用越准。

## 3. 整体架构总图

```
┌─────────────────────────────────────────────────────────────┐
│  DCS 测点数据流（生产）/ 模拟测点生成器（演示）              │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  监测 Agent（感知层，24h 常驻，规则引擎零算力）              │
│  阈值越限 / 滑动窗口趋势 / 变化率 / 跨测点关联               │
│  告警去抖 + 双条件归并（时间窗口+设备关联矩阵）→ 事件时间线  │
└──────────────────────────┬──────────────────────────────────┘
                           ▼ 缺陷事件（设备+参数+严重度+时间线）
┌─────────────────────────────────────────────────────────────┐
│  主控 Agent / Orchestrator（编排整合层）                     │
│  动态激活班组专家（Send API 并行）→ 整合研判素材            │
│  → 梳理分歧要点 → 生成结构化参考处置方案                    │
└──────┬──────────────────────┬────────────────────┬──────────┘
       ▼ 并行                 ▼ 并行               ▼
┌──────────────┐   ┌──────────────┐      ┌──────────────────┐
│ 班组专家×5   │   │ 两票辅助 Agent│      │ 复盘 Agent       │
│ 检索诊断层   │   │ 合规层        │      │ 学习层           │
│ 各自知识库   │   │ 填票防错      │      │ 人工准入入库     │
│ 诊断意见     │   │ 常见票型辅助  │      │ 索引增量更新     │
└──────────────┘   └──────────────┘      └──────────────────┘
       │                  │                       │
       ▼                  ▼                       ▼
┌─────────────────────────────────────────────────────────────┐
│  分级人工确认节点（interrupt）：低风险自动放行/高风险每步确认 │
│  ① 现场核实诊断 ② 确认处置方案 ③ 草稿定稿确认 ④ 复盘入库   │
└─────────────────────────────────────────────────────────────┘
```

支撑设施：模型网关（双模式）、知识库（混合检索 + Milvus）、MySQL 业务库、Langfuse 链路追踪（审计）、checkpoint 断点续跑。

## 4. 六大 Agent 模块详述

### 4.1 监测 Agent（感知层）

- 功能：24h 常驻消费 DCS 测点数据流，规则引擎检测（阈值越限、滑动窗口趋势、变化率、跨测点关联）；异常自动建档缺陷记录（设备、测点、异常值、严重度、时间戳）；告警去抖与归并，输出事件时间线。
- 作用：系统唯一数据入口，把"参数变化"翻译为"缺陷事件"。
- 价值：趋势预警（未到阈值提前提示）、跨测点关联（组合信号早发现）、事件收敛（告警风暴归并为一个事件）。
- 修正 1（边界）：**趋势预警仅作为运行提示，不触发正式故障会诊流程；只有越限或组合异常才启动完整多 Agent 诊断**，避免轻微趋势波动频繁唤醒 LLM 消耗算力。
- 为什么：DCS 是保护级系统不能动（工业边界），监测 Agent 是增值分析层；规则常驻零成本，LLM 按需唤醒。
- 真实场景：凌晨磨煤机 2# 轴承温度 70°C 匀速爬升未到 85°C 阈值 → 趋势预警推送运行值班；温度+振动组合异常 → 触发完整诊断。

### 4.2 主控 Agent / Orchestrator（编排整合层）

- 功能：接收缺陷事件 → 按设备归属动态激活对应班组专家（Send API 并行）→ 收集专家意见 → 整合多专家研判素材、梳理分歧要点、生成结构化参考处置方案。
- 作用：系统的"值班长"——路由、调度、整合、兜底。
- 修正 2（边界）：**主控不做最终结论判定，只做信息整合；最终结论由运维人员决定**。专家意见分歧时不自行裁决，输出分歧点清单（各自依据）交人工裁决。
- 价值：动态路由只唤醒相关班组（省算力）；多视角融合；单点整合保证流程可控可审计。
- 为什么：Orchestrator-Worker 模式适配强流程工业业务；"无 AI 决策"是电力安全底线。
- 真实场景：磨煤机缺陷激活锅炉+热控专家；锅炉专家判机械故障、热控专家判仪表误报 → 主控输出分歧点清单给运行人员。

### 4.3 班组专家 Agent ×5（检索诊断层）

- 功能：锅炉/汽机/电气/热控/输煤五个专家，各自绑定本班组知识库（BM25+BGE-M3 混合检索 + Reranker 精排）与历史案例工具，并行检索，输出诊断意见：可能原因排序 + 验证方法 + 影响评估 + 知识库处置参考条目 + 置信度 + 来源引用。
- 作用：各专业"老师傅"，提供领域证据。
- 价值：知识隔离（五个独立知识库，专业不串味）；来源可追溯（规则质检强制引用来源）；专业术语失配问题靠混合检索解决。
- 为什么：私有化模型不联网，知识全部来自库内；固定流程（检索→分析→输出）可控，不搞自由 ReAct 绕圈。
- 真实场景：汽轮机 1# 瓦温度异常 → 汽机专家检索汽机库，输出"轴瓦磨损/油膜振荡/润滑油质劣化"按概率排序，附验证方法与规程条目。

### 4.4 两票辅助 Agent（合规层）

- 功能：新人填票防错——检查工作票/操作票草稿：必填项完整性、危险点分析是否覆盖作业全流程、安全措施是否到位、动火/受限空间关联证件（动火证、气体检测记录）；常见票型库（磨煤机检修、一次风机振动、干渣机检修等典型类型）辅助生成草稿，变量预填、人工确认。
- 作用：新员工的"填票老师"，降低填错票引发事故的风险。
- 价值：老师傅不在场时新人也有可靠参考；票面标准化减少退票返工；危险点漏项拦截在开票前。
- 边界：**不介入真实 ERP 开票审核流程**（ERP 本身具备，重复造轮子）；定位是"开票前的辅助检查"，开票走真实流程。只做常见票型，不做全量模板。
- 真实场景：新员工填动火工作票 → Agent 提示"缺少动火证编号、受限空间未附气体检测记录、危险点未覆盖作业恢复阶段" → 补全后人工确认，再走 ERP 正式流程。

### 4.5 复盘 Agent（学习层）

- 功能：监测确认参数恢复正常后触发复盘——维护人员人工确认"故障原因、处置方案、执行结果是否正确、有无补充"→ 确认通过后自动清洗、入库、索引增量更新 → 提高后续同类告警的检索匹配度。
- 作用：系统记忆与成长机制。
- 价值：知识闭环（越用越准）；质量门控（人工确认后才入库，防前序判断错误污染知识库）。
- 为什么：不能直接引用前序 Agent 判断入库；入库效果表述为"提高后续匹配度"而非"直接命中"。
- 真实场景：磨煤机检修完成参数恢复 → 班长确认"轴承磨损、更换方案有效" → 案例归档 → 下次同类告警该案例排在最前。

### 4.6 分级人工确认节点（人机协同）

- 功能：低风险自动放行，高风险每步确认。关键节点：① 现场核实诊断 ② 确认处置方案 ③ 两票草稿定稿确认（确认后由填票人/负责人在 ERP 系统人工完成正式开票，系统不提交/不签发/不参与）④ 复盘入库。
- 作用：人机边界——AI 永远只给建议。
- 价值：电力安全零容错，人拍板是底线；符合监管审计要求。
- 为什么：LangGraph interrupt 原生支持；分级设计避免全流程确认烦死人的问题。
- 真实场景：诊断建议"需停运检修"——系统不执行，只把建议和依据呈给运行人员，人现场核实后决定。

## 5. LangGraph 状态机结构

```
START
  → monitor_event（事件入口：缺陷事件）
  → route_experts（动态激活 1-2 个班组专家，Send API 并行 fan-out）
      → expert_retrieve（各专家检索知识库+历史案例）[并行，fan-in 汇聚]
  → aggregate（主控整合：研判素材+分歧清单+参考方案）
  → rule_qc（规则质检：来源引用/设备+参数+班组三要素覆盖）
  → human_confirm_1（interrupt：现场核实诊断，分级放行）
  → plan_confirm（interrupt：确认处置方案）
  → if 需检修 → ticket_assist（两票辅助：草稿检查/常见票型）
      → human_confirm_2（interrupt：草稿定稿确认，走 ERP 前）
  → execute_track（消缺执行状态跟踪）
  → monitor_verify（监测确认参数恢复正常）
  → review（复盘 Agent：总结+人工准入入库）
END
```

**并行分支说明**：
- 图内并行：`route_experts` 通过 Send API 按班组动态 fan-out（只激活相关 1-2 个专家），`expert_retrieve` 并行执行后 fan-in 汇聚到 `aggregate`。
- 图间并发：每个缺陷事件是独立图实例（asyncio 并发），多告警同时处理，按严重度优先级调度。

**Interrupt 断点配置说明**：
- 人工节点用 `interrupt()` 挂起执行：图暂停在 human_confirm 节点，返回待办给前端/人工审批界面，运行人员处理（确认/驳回/补充）后，通过 checkpoint 注入结果恢复执行。
- checkpoint 使用持久化 saver（开发 MemorySaver，生产 SqliteSaver/PostgresSaver），保证：① 流程中断不丢失状态；② 服务重启可断点续跑；③ 每次人工操作留痕（审计要求）。
- 分级策略：低风险缺陷人工节点自动放行（规则判定跳过 interrupt）；高风险（动火、受限空间、重大设备）强制 interrupt 等待人工。

## 6. 告警预处理完整逻辑

1. **防抖（去抖）**：同一设备同一测点时间窗内（如 5 分钟）重复告警合并（死区抑制），防止一次故障刷屏、触发无意义诊断流程。
2. **双条件归并**：候选根因告警 A 发生后，时间窗口内（5~15 分钟可配置）出现的告警 B，必须同时满足设备关联（工艺上下游/共用轴承油站/DCS 连锁逻辑表）才归并为同一事件。双条件缺一不可：只按时间会误并（同时刻独立故障），只按关联会漏并（关联设备不同时段故障）。
3. **时序事件线**：归并结果输出时间线（"00:03 温度告警 → 00:04 跳闸 → 00:05 连锁报警"），作为诊断上下文传给专家——先后顺序有诊断意义（先振动后温度=机械磨损，先温度后振动=润滑恶化）。
4. **优先级调度**：并发告警按严重度（重大/较大/一般）排序处理，重大告警先启动诊断流程，推理资源经 vLLM 并发批处理 + 队列限流控制。

## 7. 两层并发实现方案

1. **图内并行（单实例）**：一个诊断流程内，主控通过 Send API 动态 fan-out 唤醒多个班组专家，各专家并行执行（检索+分析），fan-in 汇聚。收益：单流程延迟从"串行加法"变为"最长路径"，不增加任何 LLM 调用次数。
2. **实例级异步并发（多实例）**：多个独立告警同时触发多个完整诊断流程实例，基于 asyncio 并发执行（每个实例是独立的异步图运行），FastAPI 并发请求/事件并发消费承载。配套优先级调度 + 去抖归并，防止并发风暴。
3. **两层叠加**：多告警各自并行（实例级）+ 每个告警内部多专家并行（图内），构成完整的并发矩阵。生产形态：监测 Agent 常驻服务 + 事件队列 + 工作线程池/异步任务。

## 8. 知识库增量闭环（复盘 Agent 人工准入入库规则）

1. 告警自动建档：监测 Agent 检测到异常时，告警本身即缺陷记录，自动进入流程（无外部导入、无人工导出）。
2. 复盘触发：监测 Agent 确认参数恢复正常 → 复盘 Agent 生成复盘摘要（故障现象/原因/方案/结果/时间线）。
3. **人工准入规则（硬门控）**：维护人员必须确认"故障原因、处置方案、执行结果是否正确、有无补充"——确认通过才允许入库；不通过则标记待修正/丢弃。**防止前序 Agent 判断错误污染知识库**。
4. 入库流水线：人工确认通过 → 清洗 → 结构化 → 写 MySQL 案例表 → Milvus 索引增量 insert → BM25 增量 doc → 打知识库版本号。
5. 质量保障：规则校验必填项 + 与已有条目查重合并（同故障模式归并、新方案补充旧条目）+ 版本回滚（索引异常一键回退上一版本）。
6. 检索侧：新案例时效加权（处置方案可能更新）；效果表述为"提高后续同类告警检索匹配度"，不称"直接命中"。
7. 冷启动兜底：新缺陷无历史案例时，走规程检索 + LLM 推理生成临时参考，标注"未经验证"。

## 9. 底层支撑组件

1. **模型网关（统一 LLM Factory）**：统一 LLM Factory 接口——全程本地私有化部署：本地 72B + vLLM 批量并发推理，断网运行（电力等保合规亮点：数据不出域是硬性合规要求）。模型无关：同一套代码改配置切换（模型升级/换型只改配置不动代码）。
2. **Langfuse 链路追踪（审计）**：核心价值是**故障事后溯源**——核查某条诊断结论由哪段规程、哪条历史案例支撑，满足电厂事故复盘审计要求；同时是开发调试工具与面试演示（展示每个 Agent 的调用链）。
3. **向量检索架构**：BM25（jieba 稀疏）+ BGE-M3（稠密 1024 维 + 稀疏向量）双路混合检索 → 融合 → BGE-Reranker 精排；Milvus 支撑五班组多知识库分区 + 案例增量写入（生产形态；演示可降级 FAISS 单库）。
4. **存储**：MySQL 业务库（票记录、告警日志、缺陷台账、复盘案例）、时序数据落电厂实时库（演示用模拟流）、checkpoint 持久化存储（SqliteSaver/PostgresSaver）。

## 10. 工程部署方案

### 10.1 单机演示方案（开发/面试环境）

- 组件：Windows 单机（演示环境）+ 本地私有化模型（vLLM 推理，模型网关配置）+ 模拟测点生成器（可快进、可注入故障工况）+ Milvus（或演示降级 FAISS）+ SQLite/MySQL + Streamlit 演示面板 + Langfuse（Docker 可选）。
- Docker Compose：mysql + milvus（可选）+ langfuse（可选）+ backend + streamlit，一键拉起。
- 演示脚本：一条龙闭环（告警→会诊→方案→两票草稿→人工确认→消缺→参数恢复→复盘入库）。

### 10.2 电厂部署方案（生产设计口径）

- 硬件：内网服务器（GPU 多卡）+ 72B 模型 + vLLM 并发推理，全程断网。
- 数据接入：DCS/实时库通过 OPC UA/Modbus 接口 → 采集服务 → 时序库 → 监测 Agent 常驻消费。
- 高可用：监测 Agent 双机冗余/看门狗自愈，断点续传不丢数据。
- 安全：内网隔离、访问控制、操作留痕（人工节点日志 + Langfuse 溯源），满足等保与事故复盘审计。

## 11. 开发里程碑拆解

| 阶段 | 内容 | 周期 | 状态 |
|---|---|---|---|
| M1 | 工程重构：项目改名、UTF-8、目录规整、模型网关、知识库导入（复用 dianchang 资产） | 3-4 天 | ✅ 完成 |
| M2 | 监测 Agent + 告警去抖/归并 + 模拟测点生成器 | 3 天 | ✅ 完成（7 测试） |
| M3 | 班组专家×5 + 主控整合 + 并行链路（核心会诊） | 4-5 天 | ✅ 完成（8+1 测试，端到端验证） |
| M4 | 两票辅助 Agent + 分级人工确认（interrupt + checkpoint） | 3-4 天 | ✅ 完成（11 测试） |
| M5 | 复盘闭环 + 增量更新流水线 + 评测脚本（评估集） | 3-4 天 | ✅ 完成（17 测试，检索 Top-5 100%） |
| M6 | Streamlit 演示 + Langfuse 追踪 + 一条龙 demo 脚本 | 3 天 | ✅ 完成 |
| M7 | Docker 化 + pytest + 文档 + GitHub 整理 | 3 天 | 进行中 |

总周期约 3-4 周，M3/M4 为核心，可并行压缩。

## 12. 代码仓库规整清单

1. 统一 UTF-8 编码；删除课程痕迹（01_day01 目录、EduAgent 命名、课程风格注释）。
2. 目录结构：agents/（各 Agent 模块）、graphs/（LangGraph 图）、tools/（检索/模拟器/规则）、data/（脱敏样本，不含涉密）、eval/（评测集+脚本）、docs/（架构图+部署文档）、web/（Streamlit）。
3. README：业务说明（工作口吻）、架构图、快速启动、部署文档、评测说明。
4. 模拟数据统一标注"脱敏模拟数据"；不编造准确率/收益硬指标（评测指标必须有脚本支撑）。
5. 内部材料（INTERNAL.md）严禁上 GitHub；.gitignore 排除密钥、数据、内部文档。
6. 代码规范：类型注解、结构化日志、异常处理、配置 YAML 化、pytest 覆盖核心链路。

## 13. 高频面试问答预案

### 13.1 业务侧

**Q1：操盘台/DCS 已经实时监控参数了，监测 Agent 还有什么用？**
A：DCS 报警是"事后单点阈值"的被动监控（超限才响，响时故障已发生），且是保护级系统不能动。监测 Agent 是增值分析层：趋势预警（未到阈值预判"照此趋势 2 小时后超限"）、跨测点关联（温度+振动+油压组合早发现故障前兆）、事件收敛（告警风暴归并成事件，配时间线）。DCS 告诉我"现在出事了"，监测 Agent 告诉我"快要出事了、为什么、该怎么办"。

**Q2：为什么需要多 Agent？单 Agent 不行吗？**
A：角色异构——感知（规则）、检索诊断（LLM+知识库）、整合（LLM）、合规（规则+LLM）、学习（流程+人工门控）是不同性质的工作，职责隔离保证每个环节可控；跨班组故障需要多视角证据（机械/电气/控制各说各的），单 Agent 无法同时持有五个专业知识库且保证不串味；并行检索降低延迟。

**Q3：这是真的多 Agent 吗？还是把模块叫成 Agent？**
A：编排式多 Agent（Orchestrated），不是涌现式自主多 Agent——这是工业选择：可控、可审计、可预测。每个 Agent 有独立推理上下文（各自知识库）、独立输出契约（结构化意见）、真实信息交互（意见传递、分歧呈现），区别于纯函数改名。涌现式自由对话在电力场景是不可接受的（不可控、出错没人负责）。

**Q4：工作票审核 ERP 本身就有，为什么还做两票 Agent？**
A：系统不介入 ERP 开票审核流程（那是重复造轮子）。定位是"开票前的辅助检查"：新人填票防错（必填项、危险点完整性、安全措施、关联证件校验），常见票型辅助生成草稿、变量预填人工确认。解决的是"新人不会填、漏项引发事故"的问题，开票仍走真实 ERP 流程。

**Q5：AI 会不会替代运行人员？**
A：不会，设计上禁止。铁律"无 AI 决策"：所有输出是参考，最终结论由人决定；分级人工确认（interrupt 强制介入）；复盘入库也必须人工准入。AI 的价值是减轻盯屏负担、提供多视角参考，不是替代判断。

**Q6：数据从哪来？涉密怎么办？**
A：生产对接脱敏 DCS 接口；演示用同规格模拟工况数据 + 脱敏历史案例（如实标注）。全程断网私有化部署，数据不出域，符合电力等保合规。

### 13.2 LangGraph 技术侧

**Q1：为什么选 LangGraph 不用 AutoGen？**
A：业务是强固定流程（告警→会诊→方案→两票→复盘）+ 强制人工介入，状态机架构天然适配：显式节点/边、可控流转、可审计；interrupt 原生支持人机协同；Send API 原生支持动态并行；checkpoint 原生支持断点续跑。AutoGen 的自由对话协作适合开放场景，不适合工业强流程约束。

**Q2：interrupt 人机协同怎么实现的？**
A：人工节点调用 interrupt() 挂起图执行，状态通过 checkpoint 持久化；前端展示待办（诊断结论/方案/票草稿），人工处理（确认/驳回/补充）后把结果注入图状态，从断点恢复执行。每次人工操作留痕，满足审计。

**Q3：Send API 并行怎么用的？**
A：route_experts 节点根据设备归属构造 Send 对象数组，动态 fan-out 唤醒 1-2 个班组专家；各专家并行执行检索+分析；fan-in 汇聚到 aggregate。并行不增加 LLM 调用次数，只降低延迟。

**Q4：图的状态怎么管理？**
A：TypedDict 定义共享 State：缺陷信息（设备/参数/严重度/时间线）、各专家意见（独立字段，互不覆盖）、主控整合结果、人工确认记录。checkpoint 持久化，服务重启可恢复。

**Q5：多实例并发怎么处理？**
A：每个缺陷事件启动独立图实例（asyncio 并发），FastAPI 并发请求承载；配套告警去抖/归并/优先级调度防止并发风暴；生产用持久化 checkpoint，实例状态可恢复。

**Q6：结构化输出怎么保证？**
A：Pydantic Schema + with_structured_output 约束（诊断意见、复盘摘要、票草稿均为固定 JSON 结构）；规则质检兜底（必填字段、来源引用、三要素覆盖）；失败重试（tenacity）。

### 13.3 落地踩坑侧

**Q1：LLM 幻觉/诊断错了怎么办？**
A：三层防线：规则质检（强制来源引用、必填校验）拦截结构性问题；人工确认节点拦截语义错误（诊断、方案都要人拍板）；复盘人工准入保证错误判断不污染知识库。同时错误案例也记录（负样本），复盘时人可标注纠正。

**Q2：成本怎么控制？**
A：规则常驻、LLM 按需唤醒（趋势预警不触发诊断，只有越限/组合异常才启动）；动态路由只激活相关 1-2 个班组专家；分级确认减少无效人工轮次；检索缓存。生产切私有化后无 API 费用，仅算力成本。

**Q3：延迟问题？**
A：并行检索（最长路径而非累加）；只激活相关专家；Milvus 检索毫秒级；LLM 调用是主要延迟，通过模型选型（推理速度与效果平衡）和并行掩盖。

**Q4：知识库被污染怎么办？**
A：人工准入硬门控（复盘确认才入库）；查重合并（同故障模式归并）；版本号管理 + 一键回滚；冷启动条目标注"未经验证"。

**Q5：断网私有化模型效果不如 API 大模型怎么办？**
A：72B 级模型在垂直场景效果足够（华润等央企即用 72B 承担常规业务）；垂直知识库 + 混合检索 + Reranker 把"模型知识"转移到"检索质量"，检索强则答案准；关键输出走结构化 + 规则校验，不依赖模型自由发挥。

**Q6：个人电脑怎么演示？**
A：演示环境与生产同一套私有化模型栈——本地 vLLM 部署模型，编排逻辑（LangGraph 图、检索、规则）与推理同机运行；模型规模按演示机算力调整（模型网关配置）。演示的是架构和流程，同时验证私有化部署可行性。
